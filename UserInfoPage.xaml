<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Shell_App.UserInfoPage"
             xmlns:vm="clr-namespace:Shell_App.Models.ViewModels"
             xmlns:models="clr-namespace:Shell_App.Models"
             x:DataType="vm:UserModelView"
             Title="UserInfoPage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".5*"/>
            <ColumnDefinition Width=".5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Text="User Id:" FontSize="20" Grid.Column="0" Grid.Row="0"/>
        <Label Text="User Name:" FontSize="20" Grid.Column="0" Grid.Row="1"/>
        <Label Text="Password:" FontSize="20" Grid.Column="0" Grid.Row="2"/>
        <Label Text="Admin status:" FontSize="20" Grid.Column="0" Grid.Row="3"/>
        <Entry Text="{Binding CurrentUser.Id}" Grid.Column="1" Grid.Row="0"/>
        <Entry Text="{Binding CurrentUser.Name}" Grid.Column="1" Grid.Row="1"/>
        <Entry Text="{Binding CurrentUser.Password}" Grid.Column="1" Grid.Row="2"/>
        <Switch/>
    </Grid>
</ContentPage>